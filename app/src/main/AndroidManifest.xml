<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Health Connect"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NFC.NoActionBar"
        tools:targetApi="31">
        <activity
            android:name=".patient.patient_medical"
            android:exported="false" />
        <activity
            android:name=".patient.patient_personal"
            android:exported="false" />
        <activity
            android:name=".patient.patient_signin"
            android:exported="false" />
        <activity
            android:name=".Register"
            android:exported="false" />
        <activity
            android:name=".hospital.HOSPITAL"
            android:exported="false"
            android:label="@string/title_activity_hospital"
            android:theme="@style/Theme.NFC"/>
        <activity
            android:name=".patient.MainActivity"
            android:exported="true"
            android:label="@string/app_name" />
        <activity
            android:name=".splashscreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>